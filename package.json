{
  "name": "users-javiergarciamartin-documents-trazzo360-web-astro",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "node scripts/check-placeholders.mjs",
    "audit:content": "node scripts/audit-content.mjs",
    "audit:schema": "node scripts/audit-schema.mjs",
    "audit:links": "node scripts/audit-links.mjs",
    "audit": "npm run audit:content && npm run audit:schema && npm run audit:links",
    "preflight": "npm run build && npm run check && npm run audit"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.3.1",
    "astro": "^7.0.3",
    "tailwindcss": "^4.3.1"
  }
}

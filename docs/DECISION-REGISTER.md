# DECISION-REGISTER — Registro de Decisiones Estructurales · Trazzo360

> Registro de las decisiones estructurales aprobadas y su historial.
> Su lugar en la jerarquía lo fija `CLAUDE.md`.
> Ante cualquier duda sobre el "por qué" de una restricción o convención: buscar aquí primero.
>
> **Estados:** `APPROVED` (vigente) · `SUPERSEDED` (superado por decisión posterior) · `REJECTED` (descartado) · `PENDING` (decisión pendiente)

---

## Índice de Áreas

- [A. Datos e Integridad](#a-datos-e-integridad)
- [B. Copy y Voz Editorial](#b-copy-y-voz-editorial)
- [C. Arquitectura y SEO](#c-arquitectura-y-seo)
- [D. Diseño Visual](#d-diseño-visual)
- [E. Técnica y Código](#e-técnica-y-código)

---

## A. Datos e Integridad

| ID | Fase | Decisión | Estado | Fuente | Notas |
|---|---|---|---|---|---|
| DEC-A01 | Lote A | `calculator.ts` es la única fuente de verdad para precios y plazos. Ningún dato en content files puede contradecirlo. | **APPROVED** | CLAUDE.md §1 | Antes existían discrepancias entre content files y calculator. Lote A los alineó. |
| DEC-A02 | Lote A | `priceFrom` baños esencial = '2.800' (rango hasta 4m²: 2.800–3.800€). El dato anterior '3.200' era incorrecto. | **APPROVED** | calculator.ts | Supersede el valor inicial '3.200'. |
| DEC-A03 | Lote A | `priceFrom` cocinas esencial = '5.000' (rango hasta 8m²: 5.000–7.500€). El dato anterior '5.500' era incorrecto. | **APPROVED** | calculator.ts | Supersede el valor inicial '5.500'. |
| DEC-A04 | Lote D | `priceFrom` integrales esencial = '26.000' (40m² × 650€/m²). El dato anterior '18.000' no tenía base en calculator.ts. | **APPROVED** | calculator.ts | Supersede '18.000'. El mínimo real es surfaceMin=40m² × 650€. |
| DEC-A05 | Lote A | Plazos reforma cocinas esencial = 3–5 semanas. El dato '2-3 semanas' en content files era incorrecto. calculator.ts: `timelineWeeks: [3, 5]`. | **APPROVED** | calculator.ts línea 550 | Supersede '2-3 semanas' en todos los content files de cocinas. |
| DEC-A06 | Fundacional | `aggregateRating` y `Review` en JSON-LD solo se emiten si `SITE.reviewCount > 0`. No se falsifican reseñas. | **APPROVED** | CLAUDE.md §1, site.ts | La empresa no tiene reseñas reales todavía. reviewCount=0. |
| DEC-A07 | Fundacional | `AUTHOR.name = null`. El nombre del fundador no se publica hasta que la empresa esté registrada. Usar siempre 'Fundador y responsable de obra de Trazzo360'. | **APPROVED** | trust.ts, PRODUCT.md | El nombre está parametrizado para poder sustituirse sin editar múltiples archivos. |
| DEC-A08 | Fundacional | `AUTHOR.yearsInTrade = 15` son los años de experiencia de la persona, NO la edad de la empresa. Nunca escribir "15 años de empresa". | **APPROVED** | CLAUDE.md §0, trust.ts | La empresa (entidad jurídica) no tiene histórico; la persona sí. |
| DEC-A09 | Lote E · corregida 2026-09-07 | `AUTHOR.yearsInTrade` (15) **es publicable** como experiencia de la persona: «más de 15 años en obra». Lo que sigue prohibido es atribuir antigüedad, trayectoria o número de obras a Trazzo360 **como empresa**. | **APPROVED** | CLAUDE.md §0, `src/data/trust.ts` | La redacción anterior afirmaba que el dato no se renderizaba y citaba dos bloques comentados. Comprobado el 2026-09-07: se renderiza en 8 páginas (los 6 hubs de servicio vía la nota de `HeroCtaGroup`, `como-trabajamos` vía `AUTHOR.yearsInTrade`, y la calculadora), y los números de línea que citaba apuntaban a otro contenido. CLAUDE.md §0 autoriza expresamente el dato, así que la decisión describía como bloqueado algo que es correcto publicar. Sin números de línea: caducan en cada edición. |
| DEC-A13 | Lanzamiento | **Teléfono real en producción.** `phone: '+34614530656'`, `phoneDisplay: '614 53 06 56'`, `whatsapp: '34614530656'`. Los dos formatos difieren a propósito: `tel:` y el `telephone` del schema piden E.164 con `+`, y `wa.me` exige el prefijo de país sin `+`. | **APPROVED** | Dato facilitado por el responsable 2026-09-10 | Desbloquea 307 enlaces `tel:`, 151 enlaces de WhatsApp, el botón flotante en las 75 páginas y el `telephone` del JSON-LD. Los placeholders bloqueantes bajan de 80 a 5. **Efecto secundario que hay que confirmar:** el horario `Lun-Vie 8:00-20:00 · Sáb 9:00-14:00` estaba oculto tras el condicional `SITE.phone !== 'TU_TELEFONO'` y ahora se publica en 75 páginas y en `openingHoursSpecification`. Ese horario nunca fue confirmado por el responsable: es el valor por defecto que traía `site.ts`. |
| DEC-A14 | Lanzamiento | **Captura de lead en la calculadora, sin backend.** Bajo el resultado aparece un bloque de un campo: «¿Te enviamos este desglose por correo?». El resultado sigue visible sin dar ningún dato. Al enviar, Formspree recibe el correo del visitante **más el desglose completo en texto y la URL que reproduce el cálculo**. `formspreeId` se centraliza en `site.ts` y el bloque **sólo se renderiza con un ID real**. Añadidos estilos `@media print` para que esa URL sea imprimible como documento. | **APPROVED** | Instrucción del responsable 2026-09-10 | El sitio es estático y no tiene backend donde generar un PDF, y una skill de Claude Code corre en la máquina del responsable, no en el navegador del visitante. La pieza que lo resuelve ya existía: `updateURL()`/`initFromURL()` sincronizan **todo** el estado de la calculadora con la query string, así que la URL **es** el documento. El correo llega con el desglose ya redactado y el enlace que lo reproduce; imprimir a PDF desde ahí no requiere recalcular nada. La guarda del ID replica el patrón de `SITE.whatsapp !== 'TU_TELEFONO'`: el código queda listo y la interfaz no aparece hasta que puede funcionar. |
| DEC-A10 | Fundacional | `PROJECTS`: todos con `published: false` hasta tener fotos reales de obras propias. Las 4 entradas actuales son placeholders. | **APPROVED** | site.ts, CLAUDE.md §2 | Las fotos ilustrativas nunca pueden llevar metadatos de obra ejecutada. |
| DEC-A11 | Lote F | `SITE.address = 'Corredor del Henares, Madrid'` describe el área de servicio, no es domicilio social legal. Las páginas aviso-legal y privacidad usan `[Completar con domicilio social real]` en su lugar. | **APPROVED** | aviso-legal.astro:33, privacidad.astro:32 | LSSICE exige domicilio postal real. El placeholder es la solución hasta obtener el dato. |
| DEC-A12 | Fundacional | Ninguna cifra comercial (obras realizadas, clientes, años de empresa) puede aparecer sin confirmación explícita del titular. Si falta, usar `TODO:` visible. | **APPROVED** | CLAUDE.md §1 | La empresa está arrancando. No hay histórico que no sea el de la persona. |

---

## B. Copy y Voz Editorial

| ID | Fase | Decisión | Estado | Fuente | Notas |
|---|---|---|---|---|---|
| DEC-B01 | Fundacional | Prohibido escribir "equipo propio" o "sin subcontratas". Forma correcta: "entran siempre los mismos equipos, hay un solo interlocutor". | **APPROVED** | CLAUDE.md §0 | "Equipo propio" es afirmación sobre estructura societaria no verificada. El beneficio real (estabilidad + responsable único) se puede afirmar. |
| DEC-B02 | Lote A | **Única decisión vigente sobre em-dash.** Prohibido como marcador visual de lista y en interfaz (botones, labels, CTAs, navegación, eyebrows). Permitido dentro de prosa corrida. | **APPROVED — confirmada 2026-09-07** | CLAUDE.md §8.2 | El em-dash es un carácter editorial, incompatible con el ADN de contrato en interfaz; en prosa es tipografía legítima. Confirmada tras medir el código: 0 usos como marcador de lista y 0 en componentes o layout, de modo que el código ya la cumple por completo. Supersede DEC-B03. Lo verifica `npm run check` como advertencia. |
| DEC-B12 | Diseño FAQ | **El em-dash sale también de las FAQs y de su schema.** Amplía DEC-B02, que lo permitía en prosa corrida: en preguntas, respuestas y `FAQPage` no aparece. Los 7 incisos afectados pasan a paréntesis, que es la alternativa natural en español para un inciso enumerativo. | **APPROVED** | Instrucción del responsable 2026-09-10 | Medido antes de tocar: 14 apariciones en respuestas visibles y las mismas 14 en el schema, porque comparten fuente; 0 en preguntas. Todas eran incisos de apertura y cierre. Verificado después: 0 em-dash en preguntas, respuestas y schema, y los 101 paréntesis de apertura casan con 101 de cierre. Ampliado el mismo día a todo el sitio por DEC-B06, así que la inconsistencia que dejaba abierta está cerrada. |
| DEC-B13 | Diseño FAQ | **El em-dash no aparece en ningún punto visible del sitio.** Supersede la excepción de prosa corrida de DEC-B02 y amplía DEC-B12 más allá de las FAQs. Alternativas: paréntesis para inciso enumerativo, dos puntos para subtítulo o explicación, coma o punto para pausa. | **APPROVED** | Instrucción del responsable 2026-09-10, CLAUDE.md §8.2 | Medido antes: 136 en el fuente, pero sólo **35 visibles en 16 páginas**; el resto vivía en comentarios de código, que no se ven. Se sustituyeron por patrón: incisos cerrados a paréntesis, `Título — subtítulo` a dos puntos, y a mano los tres casos sueltos, incluido un inciso partido en dos líneas que el procesado por línea no alcanzaba. Verificado después: **0 em-dash visibles**, paréntesis balanceados en las 75 páginas y los **630 rangos con en-dash (`–`) intactos** — «8–12 m²» es uso correcto y no se tocó. |
| DEC-B03 | Lote F + Canonicalización | El em-dash estaría prohibido también dentro de prosa editorial, tratando los ficheros afectados como deuda de QA. | **SUPERSEDED 2026-09-07 por DEC-B02** | CLAUDE.md §8.2 | Era mutuamente excluyente con DEC-B02 y ambas figuraban como APPROVED, así que la regla dependía de cuál leyera cada sesión. Se resuelve a favor de DEC-B02 por decisión del responsable: el `—` en prosa corrida es tipografía legítima. Coste de la alternativa, medido el 2026-09-07: reescribir 27 ficheros y 86 apariciones del HTML publicado, cada una una decisión editorial sobre el ritmo de la frase. |
| DEC-B04 | Fundacional | North Star editorial: "La Obra que Cumple". No es un slogan publicitario sino una pregunta de control: ¿este copy habla de lo que cumplimos o de lo que prometemos? | **APPROVED** | PRODUCT.md | Contrapunto a "el sueño de tu reforma". El cliente quiere cumplimiento, no promesas. |
| DEC-B05 | Fundacional | Sistema de pagos 40/40/20 (firma / mitad de obra / entrega conforme). Dato contractual real, verificable, diferencial. | **APPROVED** | PRODUCT.md, trust.ts | El COMPROMISOS.pago-fases es garantía real. No inflar con "sin pagos por adelantado" (sí hay 40%). |
| DEC-B06 | Fundacional | Prohibidos superlativos no verificables: "el mejor", "la máxima calidad", "sin igual en el Corredor". | **APPROVED** | CLAUDE.md §6.9 | No hay datos que respalden superlativos. Sustituir siempre por concreción técnica o contractual. |
| DEC-B07 | Fundacional | `mainCity = 'Torrejón de Ardoz'`. Nunca escribir "nuestra sede en Alcalá" ni insinuar que Alcalá es la ciudad principal. | **APPROVED** | CLAUDE.md §0, site.ts | Alcalá es el municipio de mayor volumen de búsqueda pero no es la sede. Torrejón es el mainCity declarado. |
| DEC-B08 | Fundacional | Toda mención de precio va acompañada de "orientativo" y "sujeto a visita técnica". | **APPROVED** | CLAUDE.md §6.9 | Los precios de calculator.ts son rangos, no presupuestos. La visita técnica es el momento de la propuesta real. |
| DEC-B09 | Fundacional | La capa humana (vida del cliente durante la obra) y la capa técnica deben coexistir en el mismo párrafo. No separar en secciones distintas. | **APPROVED** | CLAUDE.md §6.1 | El diferencial de Trazzo360 no es técnico ni emocional: es la combinación. Cualquier bloque de solo uno de los dos está incompleto. |
| DEC-B10 | Fundacional | Prohibido "Respondemos en 24h" o similar. Suena a call center. Forma correcta: "estamos disponibles durante toda la obra" o "te aviso antes de tomar ninguna decisión". | **APPROVED** | CLAUDE.md §6.2, §6.6 | Hay una sola persona responsable, no un departamento de atención al cliente. |
| DEC-B11 | Fundacional | Prohibida la incoherencia precio cerrado + imprevistos sin el puente explicativo. Ver el párrafo canónico en CLAUDE.md §6.5. | **APPROVED** | CLAUDE.md §6.5 | El precio cerrado cubre lo visible. Los imprevistos ocultos se comunican antes de actuar, no después. Esa es la diferencia. |

---

## C. Arquitectura y SEO

| ID | Fase | Decisión | Estado | Fuente | Notas |
|---|---|---|---|---|---|
| DEC-C01 | Fundacional | Sistema S-level para contenido de combinadas: S1 (datos locales específicos), S2 (datos territoriales generales), S3 (transaccional compacto). | **APPROVED** | src/data/content/levels.ts | No todas las combinadas justifican el mismo nivel de inversión editorial. |
| DEC-C02 | Lote C | `zonas/alcala-de-henares.astro` es standalone reference, no usa el template `zonas/[slug].astro`. | **APPROVED** | levels.ts | Alcalá = S1 en todas las combinadas que la incluyen. Como zona hub también merece tratamiento diferenciado. |
| DEC-C03 | Lote D | `reforma-cocinas/alcala-de-henares.astro` es standalone reference, no usa el template `[servicio]/[barrio].astro`. | **APPROVED** | levels.ts | S1 + parque inmobiliario de los 60-70 específico de Alcalá. Golden reference para S1×cocinas. |
| DEC-C04 | Lote D | S1 aprobado para: Alcalá × reformas-integrales, Alcalá × reforma-cocinas, Alcalá × locales-comerciales, Alcalá × comunidades-vecinos. | **APPROVED** | levels.ts | Justificación: volumen de búsqueda + parque inmobiliario diferenciado + zona universitaria. |
| DEC-C05 | Lote D | S3 aprobado para: Camarma de Esteruelas × locales-comerciales. | **APPROVED** | levels.ts | Volumen de búsqueda insuficiente para justificar inversión S2. |
| DEC-C06 | Fundacional | Una sola entidad de negocio en todo el sitio con `@id: https://trazzo360.es/#business`. Ningún otro schema redeclara GeneralContractor. | **APPROVED** | CLAUDE.md §4 | Fragmentar la entidad crea señales contradictorias para Google. |
| DEC-C07 | Fundacional | Rangos editoriales de referencia por tipo de página: Hubs 2800-3200w; Zonas 1700-1900w; Combinadas 1500-1700w; Guías 1800-2500w. Orientación al redactar, no puerta de publicación. No se miden ni se reportan para cerrar una tanda. | **AMENDED 2026-09-07** | CLAUDE.md §3 | Los rangos ayudan a juzgar si a una página le falta sustancia o le sobra relleno. Enmienda: la redacción original decía que los mínimos se medían con `scripts/audit-content.mjs`; el script no contiene esos rangos ni mide boilerplate, así que la obligación nunca tuvo comprobación detrás. Se degrada a orientación en lugar de mantener un umbral obligatorio que nada verifica, y se retira el porcentaje de boilerplate por ser una métrica sin implementar. Ninguna página se recorta ni se alarga por este cambio. Ver DT-F05. |
| DEC-C08 | Fundacional | `keyword-map.md` se consulta antes de escribir cualquier encabezado. Si dos páginas compiten por la misma query, la de menor prioridad resume y enlaza a la principal. | **APPROVED** | CLAUDE.md §4 | Cannibalización de keywords = señal negativa. Una keyword, una URL. |
| DEC-C09 | Lote E | noindex en: /gracias/, /aviso-legal/, /privacidad/, /cookies/, 404.html. | **APPROVED — recuento pendiente de comprobar** | PRE-LAUNCH-CHECKLIST | Resuelto en origen el 2026-09-07: se eliminaron `src/pages/lab/` y `servicios/reformas-integrales-lab.astro`, de modo que ya no existe página de laboratorio que filtrar. Con ellas se retiraron el filtro `/lab/` del sitemap, la integración `exclude-lab-in-production` de `astro.config.mjs` y el `Disallow: /lab/` de `robots.txt.ts`. Recuento real del build del 2026-09-07: **75 páginas** (74 `index.html` + `404.html`) y **71 URLs en el sitemap**, verificadas leyendo el `sitemap-0.xml` generado con `PUBLIC_SITE_INDEXING=true`. La cifra anterior de 86 páginas era de antes de eliminar las 13 de laboratorio (DEC-C11). Cómo reobtenerla: `npm run build` y luego `find dist -name 'index.html' \| wc -l` para las páginas; para el sitemap, `grep -c '<loc>' dist/sitemap-0.xml` con la indexación activada, o aplicar el filtro de `astro.config.mjs` al listado del build cuando esté desactivada. |
| DEC-C10 | CRO 4.1 | **Estado registrado, no norma.** Los 6 hubs de servicio presentan hoy el precio en la parte alta del body, antes de compromisos y proceso, en lugar del orden descrito en `ANATOMIAS-CANONICAS.md` § «Secciones del body — orden canónico». | **ESTADO REGISTRADO — sin aprobación del orden exacto** | Sesión 2026-09-06 | Lo aprobado explícitamente fue el objetivo (subir el precio para mejorar la conversión), no la secuencia concreta que resultó. Se registra la disposición existente para que quede trazada, no para convertirla en obligatoria: no se extiende a hubs nuevos ni a otras familias de página. Materia en disputa con `ANATOMIAS-CANONICAS.md`: mientras dure, se conserva la implementación actual y no se reordena en ninguna de las dos direcciones sin instrucción explícita. |
| DEC-C11 | Saneamiento | Eliminadas las 13 páginas de laboratorio: `src/pages/lab/` (12) y `src/pages/servicios/reformas-integrales-lab.astro`. Con ellas, el filtro `/lab/` del sitemap, la integración `exclude-lab-in-production` y el `Disallow: /lab/` de robots. | **APPROVED** | Instrucción explícita del responsable, 2026-09-07 | Se construían y publicaban, y eran el origen de los errores críticos de auditoría. `reformas-integrales-lab.astro` era además un casi-duplicado de `servicios/reformas-integrales.astro` que sí entraba en el sitemap, porque el filtro sólo excluía `/lab/` y no el sufijo `-lab`. Verificado por grep antes de borrar: ninguna página de producción las enlazaba, ni en el fuente ni en el `dist/` anterior. Recuperables con `git revert`. |

---

## D. Diseño Visual

| ID | Fase | Decisión | Estado | Fuente | Notas |
|---|---|---|---|---|---|
| DEC-D01 | Fundacional · ampliada 2026-09-08 | **Radio cero en todo el sistema** (`--radius: 0px`). **Excepción nominal, dos ficheros y un valor cada uno:** `src/components/WhatsAppBtn.astro` con `rounded-full` (FAB flotante) y `src/components/Header.astro` con `rounded-sm` (botón de la barra). Ningún otro `rounded-` en ninguna parte, **ni siquiera un valor distinto dentro de esos dos ficheros**. | **APPROVED** | DESIGN-BLUEPRINT.md + `scripts/audit-design.mjs` | El botón de WhatsApp es elemento de plataforma, no componente del sistema, y ya tenía excepción por el verde `#25D366`. No se alinean a un mismo radio porque cumplen funciones distintas: el flotante es un FAB, donde la circularidad **es** la convención; el de la barra es un botón inline en una fila de navegación, y `rounded-full` lo convertiría en píldora, una forma que sí se propaga visualmente. `rounded-sm` es la mínima diferenciación frente al CTA de marca. La exención se declara por fichero **y** valor: exentar el fichero entero habría dejado sin vigilar los otros 36 elementos con clase de `Header.astro`. |
| DEC-D02 | Lote A | `bg-[#A85535]` eliminado como color de fondo de sección completa. CTA final = `bg-[#111827]`. | **APPROVED** | VISUAL-ROLLOUT-MAP.md §2 — La Regla del Acento Escaso | La terracota como fondo de sección viola el límite del 10% del viewport. Solo como acento puntual. Supersede implementación inicial. |
| DEC-D03 | Fundacional | Tipografía Display (3–4.5rem / peso 900 / tracking -0.02em) solo en H1 del hero. | **SUPERSEDED 2026-09-07 por DEC-D16** | VISUAL-ROLLOUT-MAP.md §2 — La Regla del Único Display | El principio de un solo Display por página sigue vigente y se conserva en DEC-D16. Lo que queda superado son las cifras: el código dejó atrás el rango de 3–4.5rem. |
| DEC-D04 | Fundacional | No italic/cursiva en ningún elemento. Ni énfasis, ni citas, ni decorativo. | **APPROVED** | VISUAL-ROLLOUT-MAP.md §2 — La Regla del Cero Italic | La cursiva connota subjetividad, opinión, ornamento. Incompatible con el ADN de contrato objetivo. |
| DEC-D05 | Fundacional | No `box-shadow` en elementos que no flotan sobre la página (tarjetas, secciones, etc.). Excepciones: header `shadow-sm`, dropdowns nav `shadow-xl`. | **APPROVED** | VISUAL-ROLLOUT-MAP.md §2 — La Regla del Plano por Defecto | La sombra implica elevación, que implica jerarquía especial. Si todo tiene sombra, la sombra pierde significado. |
| DEC-D06 | Lote A | No `hover:shadow-md` ni `hover:shadow-sm` en cards. Eliminar de todos los templates. | **APPROVED** | Lote A audit | Hover shadow convierte cada tarjeta en elemento flotante potencial. Viola la regla del plano. Supersede implementación inicial. |
| DEC-D07 | Fundacional | `#25D366` (verde WhatsApp) solo para elementos de WhatsApp. No usarlo en ningún otro contexto. | **APPROVED** | VISUAL-ROLLOUT-MAP.md §2 — La Regla del Verde Cerrado | El verde #25D366 tiene carga semántica unívoca (WhatsApp). Usarlo en otros contextos contamina esa señal. |
| DEC-D08 | Fundacional | Terracota (#A85535) < 10% de cualquier viewport en cualquier estado. | **APPROVED** | VISUAL-ROLLOUT-MAP.md §2 — La Regla del Acento Escaso | La terracota como señal técnica, no como ambiente. Mayoría del viewport en neutros (#111827, #F9F7F4, #FFFFFF). |
| DEC-D09 | Lote A | Bullets de lista: cuadrado 6×6px via CSS `::before`, `border-radius: 0`, `border: 1px solid #9CA3AF`, fondo transparente. No usar `rounded-full`, no usar emojis, no usar guión. | **APPROVED** | CLAUDE.md §8, VISUAL-ROLLOUT-MAP.md §2 | El cuadrado refuerza el ADN de rectángulo comprometido. La especificación CSS está congelada en CLAUDE.md §8.1. |
| DEC-D10 | Lote A | `transition-all` eliminado. Solo `transition-colors` o `transition-opacity`. | **APPROVED** | Lote A audit | `transition-all` afecta propiedades como `width` y `height` que no necesitan transición, causando jank. |
| DEC-D11 | Lote A | Barras decorativas `w-12 h-1 bg-[#A85535]` eliminadas definitivamente de heroes y secciones. | **APPROVED** | Lote A audit — REJECTED pattern | Fragmentan la lectura sin aportar información. Violación del ADN. Supersede implementación inicial. |
| DEC-D12 | Lote A | `text-[#A85535]` eliminado de H1. El H1 es negro puro (`#111827`) o con tracking ajustado. No terracota en titulares principales. | **APPROVED** | Lote A audit | El H1 en terracota crea conflicto visual con botones terracota. Solo un elemento puede ser el acento dominante. |
| DEC-D13 | Lote A-B | Hero eyebrow sin barra decorativa. El acento visual puede ser un borde-left editorial (`border-l-4`) pero no una barra flotante separada. | **APPROVED** | Impeccable direction, Home V1 | La barra decorativa es ornamento sin función. El borde-left editorial es estructura tipográfica con función. |
| DEC-D14 | Home V1 | Hero variante A2 (sigilo con borde editorial): la eyebrow usa `border-l-4 border-[#A85535]` como único acento terracota en el hero. Sin más color en ese bloque. | **APPROVED** | Home implementation | El A2 es la variante canónica para Home. No global: solo en Home y páginas que lo justifiquen. |
| DEC-D15 | Fundacional | No más de una fuente Display por página. El sistema usa una sola familia configurada en tailwind.config. | **SUPERSEDED 2026-09-07 por DEC-D16** | VISUAL-ROLLOUT-MAP.md | Superada por dos motivos comprobados. Primero: el sistema usa **dos** familias por diseño, `--font-display` (Barlow Condensed) para titulares y `--font-body` (sistema) para cuerpo. Segundo: no existe `tailwind.config`; el proyecto usa Tailwind v4, que se configura en el bloque `@theme static` de `src/styles/global.css`. Lo que sí sigue vigente es que sólo hay una familia display. |
| DEC-D16 | Saneamiento | **Canon tipográfico vigente.** Fuente display: Barlow Condensed, cargada globalmente en `src/layouts/Layout.astro` (pesos 700/800/900). `.h1-hero`: `font-size: clamp(3.5rem, 11vw, 10rem)`, `font-weight: 900`, `letter-spacing: -0.02em`, `line-height: 0.85`. Dos familias por diseño: `--font-display` para titulares y `--font-body` (sistema) para cuerpo. `--radius: 0px` en todo el sistema. Configuración en el bloque `@theme static` de `src/styles/global.css` (Tailwind v4, sin `tailwind.config`). | **APPROVED** | DESIGN-BLUEPRINT.md + `scripts/audit-design.mjs` | Supersede DEC-D03 y DEC-D15. Es el único canon que coincide con el código y el único cuyo cumplimiento se comprueba: `npm run audit:design` valida el tracking y el line-height del H1 de hero contra las páginas de `CANON_SOURCES` (`servicios/reformas-integrales.astro` y `servicios/reforma-banos.astro`). Cualquier sesión que aplicara la tipografía de sistema descrita en los documentos superados borraría Barlow Condensed y rompería el auditor. |
| DEC-D17 | Saneamiento | **Dos ejes de referencia, no uno.** `CANON_SOURCES` (`scripts/audit-design.mjs`) manda en tipografía y componentes; las Golden References de `VISUAL-ROLLOUT-MAP.md` §8 mandan en composición. Las dos listas pueden no coincidir sin que sea un error. `V2` en §7.1 significa «composición legacy», nunca ADN tipográfico incorrecto. | **APPROVED** | Decisión del responsable 2026-09-07 | Fundir ambas listas habría perdido información: dejaríamos de saber qué páginas tienen la composición aprobada. Se nombran por separado y cada documento declara sobre qué eje manda. |
| DEC-D18 | Saneamiento | **`DESIGN.md` eliminado.** Su sección `## Components` se migró a `DESIGN-BLUEPRINT.md` como «Componentes de chrome», verificando cada valor contra el código. El resto era deuda: tipografía de sistema, `clamp(3rem, 8vw, 4.5rem)`, una escala de espaciado sin ningún token en `global.css` y un `max-w-prose` de 65-70ch frente al sistema real de cinco medidas. | **APPROVED** | Decisión del responsable 2026-09-07 | Tres valores migrados no coincidían con el código y se corrigieron: faltaba `sticky top-0` en el header, se confundían en uno solo el CTA del header y el de hero, y se afirmaba que todos los primarios cambian de fondo en hover cuando el de hero baja opacidad. El «patrón de card» que describía no existe y no se migró. **Nota sobre `impeccable`:** su `context.mjs` lee `DESIGN.md` y es probable que lo regenere; si reaparece, no tiene autoridad — la jerarquía de `CLAUDE.md` y §10 lo dejan fuera. |
| DEC-D19 | Saneamiento | **Migración de radios completada.** De las 40 divergencias detectadas por DEC-D01: **38 migradas a radio cero** y **2 exentas** (los dos botones de WhatsApp, ver DEC-D01). `audit:design` vuelve a exit 0 por radios. | **APPROVED** | `scripts/audit-design.mjs` | Desglose: `calculadora-precio-reforma.astro` 35 (23 `rounded-xl` de contenedores, 10 `rounded-lg` de chips de selección y botones, 2 `rounded-full` de la barra de progreso del desglose); `gracias.astro` 3 (círculos de icono y de numeración de pasos); `Compromisos.astro` 1. **La calculadora no se eximió pese a ser una isla visual**: eximir una página entera se propaga, y es el mismo mecanismo que produjo las categorías «V2 composición legacy». Ningún control quedó mal: el `input type="range"` no llevaba clase de radio y los chips de selección marcan su estado con `border-2` y color, no con la esquina. **`Compromisos.astro` es el único cambio con efecto visible más allá de las esquinas**: su variante `condensed` usaba un círculo terracota con check SVG, que violaba también DEC-D09; ahora consume `.list-marca` (CLAUDE.md §8) y el marcador es el cuadrado 6×6 de `::before`. Afecta a las 8 páginas de zona. Texto visible idéntico antes y después en las tres páginas comparadas. |
| DEC-D20 | Saneamiento | **Tres roles de H1 de hero, uno por familia de página.** `.h1-hero` `clamp(3.5rem, 11vw, 10rem)` para home, los 6 hubs de servicio y páginas ancla; `.h1-hero--zona` `clamp(2.25rem, 5.5vw, 4.5rem)` para los 8 hubs de zona; `.h1-hero--combinada` `clamp(2.25rem, 5.5vw, 4.25rem)` para las 47 combinadas. Declarados como tokens y clases en `global.css`, consumidos vía la prop `class` de `HeroTitle`. | **APPROVED** | `src/styles/global.css` + `scripts/audit-design.mjs` | Los valores ya estaban en el sitio con consistencia del 100% —8/8 zonas y 48/48 combinadas, sin una sola excepción— pero sueltos en la prop `size` de `HeroTitle`, sin nombre y sin auditar. No es deriva: es un sistema que nadie escribió. **Unificar zona y combinada en un solo rol se evaluó y se descartó**: fundir 4.5 y 4.25 obligaría a mover 48 páginas money para ahorrar una regla de canon, y el problema de este repositorio nunca fue tener muchos roles sino tener valores sin declarar. La jerarquía es coherente con la arquitectura SEO: el hub de servicio pesa más que la combinada. Migración real: 4 ficheros fuente, no 56 páginas, porque el valor llegaba por prop. Render verificado antes y después en una página de cada familia: texto visible idéntico y valor computado equivalente. |
| DEC-D21 | Saneamiento | **`audit-design.mjs` pasa a auditor híbrido.** Reglas tipográficas sobre `dist/` (HTML compilado, componentes resueltos); regla de radios y de notación sobre `src/`. Cada bloque de la salida declara su fuente y su denominador. | **APPROVED** | `scripts/audit-design.mjs` | Leyendo el fuente `.astro` el auditor vigilaba 4 de 74 páginas y decía «sin divergencias»: las 9 plantillas que reciben el H1 de `<HeroTitle>` no llevan `id="h-hero"` literal. Cobertura de las reglas del H1: **10% antes, 84% después** — que es el 100% de las 63 páginas que tienen hero; las 12 restantes usan `.h1-page`, un rol distinto. La regla de radios se queda en `src/` porque la excepción de DEC-D01 es nominal por fichero y esa propiedad no existe en `dist/`. La de notación también, porque el minificador emite `.12em` a partir de `0.12em` y aplicarla al compilado producía 69 falsos positivos, ninguno real. La regla «Número editorial» aplicaba en 0 ficheros: su selector buscaba `font-size:clamp(5rem` inline, la forma anterior a extraerse a `.stat-editorial`; ahora vigila que nadie sobreescriba el rol desde el elemento. Alcance nuevo verificado inyectando una divergencia en `zonas/[slug].astro`, plantilla que antes quedaba fuera: 7 páginas detectadas, exit 1. |
| DEC-D22 | Saneamiento | **`PasosIncluye.astro`: la lista «Qué incluye» pasa a componente único, en los 6 hubs.** Markup semántico `<ol>`/`<li>` con `<h3>` por ítem; prop `ordenado` para distinguir proceso cronológico (`<ol>` numerado, 4 hubs) de catálogo de alcance (`<ul>` sin números, 2 hubs). Se elimina la flecha `→`. El número pasa de `0.6875rem` a `clamp(1.5rem, 4vw, 2.5rem)` en peso 900. CSS scoped en el componente; sección 16 de `global.css` vaciada. | **APPROVED** | Decisión del responsable 2026-09-08 | Cuatro problemas, dos de ellos con efecto en SEO y accesibilidad. **Semántica:** 3 de los 4 hubs con el patrón usaban `<div>` para una lista numerada y `<p>` para el título del ítem, de modo que 17 de 24 títulos no existían como estructura para Google ni para un lector de pantalla; sólo `locales-comerciales` usaba `<ol>` y sólo `reformas-integrales` usaba `<h3>`. **Affordance falsa:** la flecha llevaba `aria-hidden`, `cursor: default` y un hover que la desplazaba y la teñía de terracota — toda la señal decía «clicable» sin serlo, en páginas de conversión. **Duplicación:** el mismo markup repetido en 4 ficheros. **Jerarquía visual:** el número era un índice de 11px. La variante sin numerar existe porque en `reforma-pisos` y `comunidades-vecinos` los ítems son zonas de la vivienda y tipos de obra, no fases: numerarlos habría afirmado una secuencia inexistente, el mismo error semántico en dirección contraria. Verificado que el copy no cambia: comparación palabra por palabra del `<main>` antes y después en los 6 hubs, con la única diferencia de las 6 flechas retiradas por hub. |
| DEC-D23 | Saneamiento | **Semántica de listas ordenadas, resto del sitio.** `ProcesoSteps.astro` pasa de `<div>`/`<p>` a `<ol>`/`<li>`/`<h3>` (componente compartido: un fichero, 2 hubs). `.fase-grid` de `reformas-integrales` pasa de `<div>` a `<ol>`/`<li>`, **sin `<h3>`**. `.vida-item__title` de `reforma-banos` pasa de `<p>` a `<h3>`, conservando `<ul>`. | **APPROVED** | Decisión del responsable 2026-09-08 | Aplica la regla de CLAUDE.md §5 fijada en DEC-D22. Dos matices deliberados. **`fase-grid` no lleva `<h3>`**: es un diagrama de fases con `aria-live` y barra de progreso, y su campo `fase` es una etiqueta de visualización, no un título de sección; añadir 6 encabezados habría metido en la jerarquía elementos que no encabezan contenido. Sí pasa a `<ol>` porque la secuencia es información real, como dice su propio copy: «la secuencia no es flexible». **`vida-list` se queda en `<ul>`**: sus 3 ítems son medidas concurrentes durante toda la obra —protección, limpieza diaria, horario—, no una cronología, así que numerarlas afirmaría un orden inexistente; sólo necesitaba el `<h3>`. Dos candidatos del inventario inicial resultaron falsos positivos al inspeccionarlos: `index.astro` ya usaba `<ol>`/`<li>`/`<h3>` correctamente, y `reforma-pisos`/`locales-comerciales` no tenían más listas numeradas que las ya migradas. Copy verificado palabra por palabra: sin cambios. |
| DEC-D24 | Diseño FAQ | **Tres mejoras en el bloque de FAQ**, que comparten 67 páginas. (1) `.faq-a` recibe `max-width: 62ch`. (2) La primera pregunta abre por defecto, con prop `abrirPrimera` para desactivarlo. (3) La apertura se anima con `faq-abrir` (opacidad + 6px), respetando `prefers-reduced-motion`. | **APPROVED** | Instrucción del responsable 2026-09-10 | Medido: la respuesta mediana son 310 caracteres y 48 de las 400 pasan de 400, a ancho completo de contenedor y sin límite de medida, cuando el proyecto ya tenía `--measure-body` para eso. Con todas cerradas, el visitante veía una lista de títulos sin una sola respuesta: el contenido estaba en el DOM para Google pero no a la vista. `<details>` no anima de forma nativa —el contenido pasa de `display:none` a visible— así que se anima la entrada del cuerpo, que es donde se notaba el salto; el cierre no se anima porque sin JS no hay forma fiable y no molestaba. **Se descartó el acordeón exclusivo** (`<details name>`): ver la nota de la decisión. |
| DEC-D25 | Diseño FAQ | **FAQ larga agrupada por tema.** `FaqAccordion` acepta `grupos` como alternativa a `faqs`. Con grupos, el subtítulo toma el `<h3>` y las preguntas bajan a `<h4>`; sin grupos, el render no cambia. Aplicado a las **11 páginas** con 9 o más preguntas. | **APPROVED** | Instrucción del responsable 2026-09-10 | Medido: 11 páginas tienen entre 9 y 12 preguntas, frente a 48 que tienen 5. No es una escala, son dos bloques con funciones distintas: en las combinadas la FAQ es apoyo, en hubs y guías es contenido principal. Doce títulos seguidos que empiezan todos por «¿Qué…», «¿Cuánto…» o «¿Cómo…» son un muro. Las 12 de baños quedan en 4 grupos siguiendo el orden de una decisión de compra: alcance y decisiones previas, durante la obra, materiales, y garantía/zona/pago. **Ni una pregunta ni una respuesta se reescribe: sólo se reordenan y se agrupan.** El `FAQPage` conserva las 12 y se aplana desde los grupos para que su orden coincida con el visible; Google no lo exige, pero una discrepancia gratuita no aporta nada. Extendido a las 11 el mismo día tras validar el criterio en el piloto. Agrupaciones por familia: los hubs de servicio siguen el recorrido de compra (alcance, obra, materiales, condiciones); las guías de precio arrancan por «Cuánto cuesta» y siguen por qué lo encarece y cómo comparar; la zona separa qué se hace, precio y trámites. **Ninguna pregunta ni respuesta se reescribe.** La calculadora necesitó tratamiento aparte: su fuente es el propio `faqSchema`, así que los grupos se derivan de él y el schema se reconstruye con el orden de los grupos en vez de aplanarlos. Verificado en las 11: recuento visible = esperado = schema, mismo orden, sin preguntas duplicadas y sin saltos de encabezado; y las 56 páginas restantes conservan la pregunta en `<h3>` sin cambios. |
| DEC-D26 | Diseño | **Dos roles de numerador para todo el sitio: `.num-paso` y `.num-indice`.** El grande —`clamp(1.5rem, 4vw, 2.5rem)`, peso 900, terracota, monoespaciada— para listas de contenido que se leen enteras; el pequeño para numeradores de apoyo dentro de un bloque denso donde el protagonista es otra cosa. Ningún numerador define ya su propia tipografía. | **APPROVED** | Instrucción del responsable 2026-09-10, `src/styles/global.css` | Había **trece tratamientos distintos** para lo mismo: seis clases entre 9 y 11px, una a 24-40px y cuatro escritas con Tailwind suelto sin clase de rol. La grande era `.incluye-n`, que agrandamos en DEC-D22, así que la inconsistencia la creó ese cambio. Resultado: **220 numeradores en 57 páginas** con la escala grande y 9 con la de apoyo (el diagrama de fases y las píldoras de coordinación, donde el número es referencia y agrandarlo rompería la rejilla de 6 columnas). Las columnas de `2rem` y `3rem` se ampliaron a `4rem` para que el número quepa. Se retiró un `style` inline de la home que habría ganado a la clase. Copy verificado idéntico en las 74 páginas. |
| DEC-D27 | Diseño | **Consolidación y semántica de bloques numerados.** `proceso-steps` estaba duplicado a mano en `locales-comerciales` y `comunidades-vecinos`: ambas pasan al componente `ProcesoSteps`, que gana prop `class`. `vida-item__title` de `reformas-integrales` pasa a `<h3>`. `criterios-list` pasa de `<div>` a `<ol>`/`<li>`. | **APPROVED** | Instrucción del responsable 2026-09-10 | La duplicación explicaba un fallo real: cuando DEC-D23 arregló `ProcesoSteps`, esas dos páginas no recibieron el arreglo porque no usaban el componente, y su título seguía en `<p>`. Consolidar elimina ~33 líneas y cierra el agujero. `vida-item__title` era un cierre a medias de DEC-D23: se migró `reforma-banos` y no `reformas-integrales`, que tiene el mismo bloque. `criterios-list` va en `<ol>` porque su propio copy dice «en el orden en que importan». **No se tocó `presupuesto-row`**: vive en un `<dl>` con `<dt>`/`<dd>`, que es lo correcto según CLAUDE.md §5 para pares término-definición; convertirlo a `<ol>` habría sido un error. |
| DEC-D28 | Diseño · CRO | **CTA de cierre de bloque en tres puntos, no en trece.** Componente `CtaInline`. Tras el proceso de obra (4 hubs) → visita técnica; tras «cómo leer un presupuesto» (1 hub) → calculadora; tras «qué se evalúa antes de cerrar el precio» (**48 combinadas**) → calculadora. Total: 52 páginas. | **APPROVED** | Instrucción del responsable 2026-09-10 | Ningún bloque numerado cerraba con llamada a la acción, pero poner una en los 260 sería ruido. Se eligen tres puntos por dónde está la cabeza del visitante al terminar de leer, no por cobertura. **El reparto se ajustó sobre la propuesta inicial:** los hubs ya acumulan de 2 a 4 enlaces a `/contacto/`, así que sólo el CTA del proceso va ahí y los otros dos van a la calculadora, que además encaja mejor —quien acaba de aprender a leer partidas quiere verlas desglosadas, y quien acaba de leer qué condiciona el precio en su municipio quiere estimarlo—. Cada CTA lleva **anchor propio y distinto**: «Empezar por la visita», «Ver el desglose por partidas», «Estimar el precio de mi caso». Verificado que ninguno de los tres se repite en ninguna página. |
| DEC-D29 | Diseño | **`cond-item` pasa a `<ol>`/`<li>` en las 48 combinadas.** Los ítems eran `<div>` hermanos sueltos en un bloque numerado 01, 02, 03 con orden real. | **APPROVED** | Instrucción del responsable 2026-09-10 | Corrección a la auditoría previa: se afirmó que estos ítems **no tenían título**, y sí lo tienen —un `<h3>` con clases Tailwind, que la detección no vio—. El problema real era sólo el contenedor. `reforma-cocinas/alcala-de-henares` tiene los cuatro ítems escritos a mano en vez de generados, y se convirtió aparte. |
| DEC-D30 | Diseño | **El marcador de lista pasa de cuadrado hueco 6×6 a barra de 10×1px.** Supersede DEC-D09 y la especificación «congelada» de CLAUDE.md §8.1, que queda actualizada. Variantes `dark` y `compromisos` pasan de `border-color` a `background`. | **APPROVED** | Instrucción del responsable 2026-09-10 | El cuadrado hueco era una forma cerrada que pedía atención propia. El sistema se apoya en líneas de 1px —separadores, bordes de tarjeta, la línea del proceso, los bordes de hover— y el marcador ahora habla ese idioma: marca sin competir. Alcance: 25 listas en 12 páginas. Alineación recalculada para el nuevo alto: `(15px × 1.55 − 1px) / 2 = 11.125px`, y `(13px × 1.5 − 1px) / 2` en la variante de convivencia. |
| DEC-D31 | Diseño | **Hover unificado en los bloques de lista: borde izquierdo terracota, sin desplazamiento.** Se propaga el patrón de `.vida-item` a `.proceso-step`, `.presupuesto-row`, `.criterio-row`, `.faq-summary` y `.price-row`. | **APPROVED** | Instrucción del responsable 2026-09-10 | Cinco bloques movían el contenido con `padding-left` al pasar el cursor. Con un número grande al lado o una línea vertical fija, ese desplazamiento se lee como un salto. El patrón de `.vida-item` —que el responsable aprobó explícitamente— no mueve nada: pinta un borde de 2px que ya existía transparente. |
| DEC-D32 | Diseño | **Línea del proceso centrada en el número.** `.proceso-steps::before` pasa de `left: 0.875rem` a `2.5rem`, el centro de la columna del numerador, y el número se centra con fondo blanco para que la línea lo atraviese sin cruzarlo. | **APPROVED** | Instrucción del responsable 2026-09-10 | La línea pasaba por la izquierda del número y se leía como un margen, no como una secuencia. Se mantiene el cero inicial (`01`, no `1`) por coherencia con `cond-num` e `incluye-n`, que ya lo usan en 52 páginas. |
| DEC-D33 | Diseño | **El bloque «vida durante la obra» tiene un patrón único y se propaga a los 5 hubs que ya tenían ese contenido.** `.vida-grid` (42% / 1fr) + `.vida-item` con numerador `01`–`05` y título en `<h3>`. Los valores del titular, la entradilla y la nota de cierre salen de los estilos inline y pasan a `.vida-title`, `.vida-lead` y `.vida-note` en `global.css`. | **APPROVED** | Instrucción del responsable 2026-09-10 | El contenido ya existía en `reforma-pisos`, `comunidades-vecinos` y `locales-comerciales`: los tres tenían exactamente la misma forma de dato —5 pares título+descripción sobre fondo oscuro— con una piel más débil (barra terracota de 4px y `<p>` como título de item, contra §5). No hubo que escribir copy: la comparación de las 74 páginas renderizadas confirma que sólo entran los numerales `01`–`05`. La extracción a CSS es la regla de `docs/PROTOCOLO-LAB.md` citada en CLAUDE.md §9: los valores aprobados se extraen a `global.css`, no se copian de página a página. Se detectó de paso que las dos implementaciones previas habían divergido en el tamaño del `<h2>` (`clamp(2.5rem,6vw,5rem)` frente a `clamp(2rem,5vw,3.5rem)`); la extracción las unifica. |
| DEC-D34 | Diseño | **La comparación «A o B» tiene un patrón único, `.decision-card`, y se propaga a las 2 páginas restantes que planteaban esa disyuntiva.** La tarjeta oscura es siempre la protagonista. Los colores de la variante `--dark` salen de los estilos inline y pasan a `global.css`. | **APPROVED** | Instrucción del responsable 2026-09-10 | El patrón vivía sólo en `reformas-integrales` y `reforma-banos`. Un barrido de encabezados con forma «X o Y» encontró exactamente dos candidatos más: `reforma-pisos` («Reforma de vivienda o reforma integral») y la guía `cuanto-cuesta-reformar-un-piso` («reforma de acabados o reforma integral»). Ambos ya eran dos tarjetas con criterios y precio: la misma información con menos jerarquía. «Reforma integral» queda como tarjeta oscura en las cuatro páginas, para que la misma disyuntiva se lea igual en todo el sitio. |
| DEC-D35 | Diseño | **Ningún marcador de lista nativo del navegador.** `list-disc` queda prohibido; las listas de contenido usan `.list-marca`. `list-decimal` se conserva sólo en listas realmente ordenadas donde el número es contenido. | **APPROVED** | Auditoría 2026-09-10 | 19 listas en 8 páginas —3 guías, 2 hubs, 3 páginas legales— pintaban el punto redondo del navegador, saltándose el sistema de bullet de CLAUDE.md §8 por completo. Es la misma regla que DEC-D30 aplicada al caso que nadie había mirado: §8 describía cómo es el marcador, pero ningún documento decía que el marcador nativo estuviera prohibido. Migración sin cambio de copy en las 74 páginas. |
| DEC-A16 | SEO | **La sección `#nosotros` de la home recupera su encabezado.** El `<p id="h-trust">` al que apuntaba `aria-labelledby` pasa a `<h2>`. | **APPROVED** | Auditoría 2026-09-10 | `<section aria-labelledby>` debe apuntar al id de un encabezado (CLAUDE.md §5). Apuntaba a un `<p>`, así que la sección más importante de la página más importante no tenía encabezado: ni para el árbol de accesibilidad ni para el rastreador. El texto ya se veía como titular; sólo estaba mal etiquetado. Único caso del sitio: el barrido de las 75 páginas no encontró otro. |
| DEC-A17 | SEO | **Las listas numeradas de las 3 guías titulan cada item con `<h3>`, no con `<p>`.** El numerador recibe `aria-hidden`. | **APPROVED** | Auditoría 2026-09-10 | CLAUDE.md §5 lo nombra explícitamente: «Nunca `<p>` para títulos de items en una lista de proceso». Los bloques «Errores caros al reformar…» ya eran `<ol>` con numerador `.num-paso`, pero sus títulos no entraban en el esquema de encabezados. Verificado que no introduce saltos de nivel en ninguna de las 75 páginas. |
| DEC-A18 | Conversión | **Las 3 guías de precios reciben una acción de contacto, sin añadir un botón más.** En el bloque de cierre, «Pedir visita técnica para mi {piso/baño/cocina}» pasa a ser la acción principal; la calculadora y el hub de servicio quedan como secundarias, y «cómo trabajamos» baja a enlace en prosa. Siguen siendo tres botones. | **APPROVED** | Instrucción del responsable 2026-09-10 | Censo de enlaces de acción por familia: hubs 5,5 · zonas 5,0 · combinadas 4,0 · **guías 0,2**. Las guías tenían 14 enlaces internos y ninguna forma de pedir nada: los tres botones del cierre llevaban a más lectura (calculadora, servicio, cómo trabajamos). El `<h2>` de ese bloque prometía «presupuesto cerrado para tu piso» y no ofrecía ninguna vía de obtenerlo. El riesgo que había que evitar era el contrario al de sobrecargar. Anchors distintos en cada guía para no repetir el «Solicitar visita técnica gratuita» de DT-F09. De paso se corrige una infracción de §4: la calculadora recibía dos anchors que solo se diferenciaban por la mayúscula inicial. |
| DEC-A19 | Conversión | **El CTA «Estimar el precio de mi caso» sale del `<nav>` de migas de pan de las 48 combinadas** y pasa al cierre de «qué se evalúa antes de cerrar el precio». | **APPROVED** | Corrección de un fallo propio, detectado por el responsable 2026-09-10 | Al añadir los CTA de DEC-D28 el bloque se insertó dentro del `<nav aria-label="Migas de pan">`, antes del hero: un CTA promocional dentro del landmark de navegación, a ~60 px del borde superior. Además su nota («Con estos condicionantes ya vistos») presuponía una sección que el visitante todavía no había leído. En su posición correcta queda a ~2.060 px, y la nota es cierta. Sólo afectaba a esta plantilla: las otras seis páginas con `CtaInline` lo tenían dentro de `<section>`. |
| DEC-D36 | Diseño | **Patrón `.catalogo-row` para catálogos término/definición.** `<dl>` con `<dt>`/`<dd>`, separadores de 1px y el hover de DEC-D31. No lleva numerador. | **APPROVED** | Fase 1 de contenido 2026-09-10 | `.criterio-row`, que es el patrón más parecido, exige una columna de numerador. Numerar un catálogo sin orden sería estructura falsa: el número afirmaría una secuencia o una jerarquía que el contenido no tiene. §5 ya dice dónde va este contenido (`<dl>`); faltaba cómo se ve. |
| DEC-A20 | Contenido | **Se renderiza contenido de zona que estaba escrito y no salía en ninguna página.** `localAngle` en las 48 combinadas; `topWorks` en los hubs de zona de plantilla. | **APPROVED** | Instrucción del responsable 2026-09-10 | La plantilla combinada usaba 3 de los 16 campos de `zones.ts`. `localAngle` (28–41 palabras por zona sobre el parque de vivienda real) y `topWorks` (4 fichas por zona) no se renderizaban en ninguna parte del sitio. **`topWorks` no va en las combinadas**, aunque la propuesta inicial lo planteara: sus 4 fichas cubren los cuatro servicios, así que en `/reforma-banos/{zona}/` tres de ellas hablarían de cocinas, integrales y locales, diluyendo el foco temático y repitiéndose idénticas en las 6 combinadas de esa zona. Su sitio es el hub de zona, donde las 4 son pertinentes y aparecen una sola vez. Efecto medido: combinadas 1.166 → 1.216 de mediana; zonas de plantilla 1.314 → 1.462. Sigue faltando para el rango de §3: eso es la Fase 2. Sin datos de `topWorks` en Azuqueca y Camarma; Alcalá no lo usa porque su página es manual y ya está en rango. |
| DEC-A21 | Copy | **Dos em-dash visibles en la calculadora, corregidos.** Uno en un texto de `qualityExcludes` y otro en el hueco de precio sin calcular. | **APPROVED** | Auditoría 2026-09-10 | Vivían dentro de `<script>`, así que los barridos anteriores sobre HTML estático no los veían, pero el JS los pinta en pantalla. El primero rompía además el patrón de sus propios hermanos de lista, que usan paréntesis. El segundo es un elemento de interfaz, prohibido explícitamente por §8.2; pasa a en-dash, que la misma regla permite. Ninguna cifra de `calculator.ts` se ha tocado. |
| DEC-A22 | Semántica | **Cinco correcciones de HTML semántico, sin un solo píxel de cambio.** `<main>` anidado de `/guias` convertido en `<div>`; 11 `<caption>` en las tablas que no lo tenían; 46 `scope="col"` en fuente (63 renderizados); 13 `<section>` con `aria-labelledby` apuntando a su encabezado; la 404 deja de emitir canonical. | **APPROVED** | Lote 1 del playbook, 2026-09-11 | **Ninguna de estas reglas es nueva: §5 ya exigía `<main>` único, `<caption>`, `<th scope>` y `aria-labelledby`, y llevaban sin cumplirse desde que se escribieron.** Lo que faltaba no era la norma sino la comprobación: ningún auditor mira semántica, así que el incumplimiento era invisible. Los 13 `<section>` tenían todos encabezado, así que no hubo caso dudoso que decidir. Los 46 `<th>` están todos en `<thead>`: se verificó uno a uno que ninguno es cabecera de fila antes de aplicar `scope="col"`. Los `<caption>` van con `sr-only`, que Tailwind emite como `position:absolute` de 1×1 px con `clip-path`, de modo que el `<thead>` sigue empezando a 0 px del borde de la tabla. La excepción de canonical se añade a §4, que era la única regla de las cinco que no contemplaba el caso. |
| DEC-A23 | SEO | **Títulos y metas dentro del rango de §4 en las 70 páginas indexables, y las 5 con `noindex` propio quedan excluidas del rango.** `/cookies` pasa a `robots="noindex, follow"` por instrucción del responsable. El título de zona y el de combinada se componen ahora con un escalón de modificadores comerciales. La meta de zona pasa a usar el `metaDesc` por municipio que ya existía en `zones.ts` sin renderizarse. | **APPROVED** | Lote 2 del playbook, 2026-09-11 | Estado previo: 6 títulos y 31 metas fuera de rango entre las indexables. **La hipótesis del playbook era falsa**: el fallback de la plantilla combinada no produce las metas largas, no se usa nunca, porque `src/data/content/*.ts` tiene las 48 escritas a mano; las largas hubo que recortarlas una a una, 102 caracteres en total. El título sí tenía un fallo de lógica: probaba sólo «· Precio Cerrado» y, cuando no cabía en 60, devolvía el título corto sin modificador, que es de donde salían los tres títulos de 45 y 46 caracteres. La meta de zona era una plantilla cuya longitud depende del nombre del municipio, así que con «Meco» (4 letras) y «San Fernando de Henares» (23) era **imposible** que las 8 entraran en rango con un texto fijo: exigía texto por zona, y ese texto ya estaba escrito en `metaDesc`. Efecto lateral asumido: 6 metas de zona que ya estaban en rango cambian de texto, a cambio de que las 8 dejen de ser la misma frase con el municipio sustituido, que es lo que §3 pide. La exclusión de las `noindex` se apoya en la sección «Interpretación» de `CLAUDE.md`: las reglas se leen por su intención, no como checklist. |
| DEC-A24 | Auditoría | **`audit:content` pasa a comprobar los rangos de §4 como error, con las páginas de `robots` propio excluidas.** Title 50–60 y meta 140–155 en las 70 indexables; en las 5 excluidas sólo se exige que title, meta y `<h1>` existan. La lista de excluidas se deriva de `src/`, no de `dist/`. | **APPROVED** | Instrucción del responsable 2026-09-11 | Los umbrales anteriores eran mucho más anchos que la regla que decían vigilar: crítico sólo por debajo de 30 caracteres de título o 80 de meta, y aviso a partir de 165. Con ellos el auditor estuvo **en verde mientras 36 metas y 8 títulos estaban fuera de rango**, que es el mismo punto ciego del Lote 1 con la semántica: la norma existía y nadie la comprobaba. La lista de excluidas se lee de `src/` porque en `dist/` no se pueden distinguir: mientras `PUBLIC_SITE_INDEXING` esté apagado todas las páginas llevan `noindex`, así que el HTML no separa «no me indexo yo» de «todavía no se indexa nada». El único dato fiable es que la página pase la prop `robots`, que gana sobre la bandera global. Es el mismo reparto híbrido de `audit-design`. Una ruta **dinámica** con `robots` propio no se puede resolver leyendo el fichero, así que el auditor aborta y lo dice en voz alta en vez de dejarla fuera en silencio. Verificado con cuatro controles: meta de 156 detectada, título de 8 detectado, `/cookies` con meta de 94 correctamente ignorada, y la guarda de rutas dinámicas disparando. |
| DEC-D37 | Diseño | **Las 18 tablas del patrón A pasan a `DataTable.astro`, con los estilos en `global.css` como `.tabla-dato`.** `caption` es prop obligatoria; el `<thead>` y el `scope="col"` los emite el componente. Las variantes de fondo, gutter, alineación, ocultar en móvil, énfasis de celda y fila destacada se declaran por prop. Los patrones B (5 tablas de la calculadora y afines), C (3 de cabecera oscura) y E (2 ya extraídas) quedan fuera. | **APPROVED** | Lote 3 del playbook, 2026-09-11 | **El censo del playbook decía 11 tablas en 5 páginas con 3 grafías: eran 28 en 10 ficheros y 4 patrones distintos.** Las 11 eran sólo las que no tenían `<caption>` en el Lote 1. Se extrae únicamente el patrón A porque B tiene otra tipografía y otra densidad, y C, además de cabecera oscura, ya usa `<th scope="row">` en el cuerpo: fusionarlos sería nivelar hacia abajo. El CSS se escribe con `@apply` de las mismas utilidades que sustituye, para que los valores sean idénticos por construcción y no por transcripción. Se conservan los dos gutters y los tres fondos: unificarlos movería 18 tablas y este lote prometía cero cambio visual. **La medición en navegador encontró tres cosas que el diff de texto no puede ver**: sobre beige la fila alterna sube a blanco en vez de bajar a gris, el separador baja a `gray-200`, y una tabla de plazos tenía dos filas de total con fondo terracota y negrita que la conversión había aplanado. Verificado con hash de estilos computados celda a celda en las 6 páginas: las 6 idénticas. |
| DEC-D38 | Diseño | **Los 49 botones del sitio pasan a `Btn.astro`: dos variantes y tres tamaños, con los estilos en `global.css` como `.btn`.** `sm` 10/20 en 12px y tracking `.05em`; `md` 12/24 y `lg` 16/32 en 13px y tracking `.08em`. Los tres en peso 700. `HeroCtaGroup` y `CtaInline` se apoyan en el sistema; `.hero-cta-primary` y `.hero-cta-secondary` desaparecen. | **APPROVED** | Lote 4 del playbook, con `lg = 16/32` elegido por el responsable 2026-09-11 | Eran **23 grafías para 49 botones**, no 18 para 44: el recuento del playbook no veía los que tenían la clase en `global.css`. Cinco paddings, dos pesos, dos tamaños de texto, bordes de 1 y 1,5 y 2 px con cuatro opacidades, y **19 botones con el fondo en un `style` inline**. En `/servicios/reforma-banos/` convivían siete tamaños distintos. **No era jerarquía sino duplicación**: el hero ya tenía componente y lo usaban los 6 hubs con 13px/700/1.04, mientras otras 4 páginas lo escribían a mano con 12px/900/1.2; el cierre de bloque ya tenía `CtaInline` con 13px/1.04 y las 13 versiones a mano eran 14px/1.4; y el CTA final era `px-12 py-5` en 3 hubs y `px-10 py-4` en los otros 3. Por eso `md` y `lg` no inventan valores: son los de `.cta-inline__link` y `.hero-cta-primary`, que ya eran canon de facto. El horizontal de `lg` (2rem) es el punto medio entre 1,75 y 2,5, para que ni el hero ni el CTA final se llevaran todo el ajuste. Se conserva una tercera variante, `inversa`, con un solo uso: el CTA final de `/como-trabajamos/` va sobre una sección terracota, donde la primaria sería invisible. Se normaliza un botón de `reforma-banos` que llevaba borde terracota sobre tarjeta oscura. **Enmienda del mismo día:** `sm` se separa de los otros dos tamaños en cuerpo y tracking. Con 13px y `.08em` el texto del botón del pie medía 263px y su columna deja 262 disponibles, así que partía en dos líneas donde antes cabía en una. Al medirlo apareció el motivo de fondo: header y pie siempre habían usado `tracking-wider` (`.05em`) frente al `tracking-widest` de los botones de contenido, y la unificación lo había aplanado. Restaurarlo devuelve el pie a una línea (291×38) y es la distinción que ya existía, no un parche. |
| DEC-D39 | Diseño | **El eyebrow se declara como dos roles y el canon se actualiza a lo que hace el código.** `.eyebrow` pasa a `0.75rem` · 700 · `0.1em` (rótulo de sección, 106 elementos) y nace `.eyebrow--min` con `0.625rem` · 900 · `0.15em` (etiqueta menor, 26). Las clases llevan sólo la firma tipográfica: el color y el margen se quedan en el punto de uso. **Supersede el valor anterior de `.eyebrow`** y elimina `.eyebrow--dark`. `audit:design` estrena tres reglas para vigilarlo. | **APPROVED** | Opción B elegida por el responsable 2026-09-11 | El diagnóstico de partida era «el canon dice una cosa y el código otra», y **estaba incompleto en tres puntos**. Primero: no eran 106 elementos en 20 grafías sino **132 en 22 firmas**, porque el recuento no veía los que llevan estilo inline. Segundo: `.eyebrow` **no se usaba en ninguna página** — `HeroTitle` usa `.h1-hero__eyebrow`, que es otra clase, así que la única referencia estaba en `SectionHeader`, que no invoca nadie. Y tercero, lo importante: **`.eyebrow` no describía el rótulo de sección sino la etiqueta menor**. El canon no estaba equivocado, estaba mal asignado, y por eso adoptarlo tal cual (opción A) habría encogido 106 rótulos para honrar un valor que describe otro rol. La deriva real estaba dentro de cada rol: 20 de los 106 rótulos de sección iban a peso 900, y viven en exactamente cuatro ficheros —las plantillas de combinada y zona más sus dos páginas manuales— etiquetando los mismos textos que en los hubs van a 700. Es el mismo patrón que el botón de hero en DEC-D38: la plantilla se escribió aparte y derivó. Cambian 26 de 132 elementos: 20 de peso, 4 de 11px a 10px y 2 sueltos de 7 y 8px. Se descarta una tercera familia, las micro-etiquetas en Courier New, que ya tiene clases propias y es otro objeto. |
| DEC-D40 | Limpieza | **Se borran 4 clases muertas de `global.css` y se adopta `.measure-tight` donde ya se estaba escribiendo su valor a mano.** Fuera `compromisos-apertura__dato`, `__sub`, `__tag` y `__valor`. Los tres `style="max-width:22ch"` de la home pasan a la clase. Se conservan `measure-lead`, `measure-hero` y `stat-editorial--dark`. | **APPROVED** | Lote 6 del playbook, 2026-09-11 | **La lista del playbook incluía 4 clases que SÍ están en uso**: los `ajuste-alto`, `ajuste-bajo`, `ajuste-medio` y `ajuste-variable` de la tabla de partidas de `reformas-integrales`. No aparecían en el grep porque la clase se compone en tiempo de ejecución, `` `ajuste-${p.ajuste.toLowerCase()}` ``, y sólo existe ya renderizada: 7 instancias en `dist/`. Es la razón por la que este lote exigía verificar cada punto antes de borrar. Las 4 que sí mueren pertenecían a una versión anterior del bloque de compromisos, que hoy se pinta con `stat-editorial`, `eyebrow` y `h3-sub`, y cuya rejilla vive en un `<style>` local de `locales-comerciales`. `.measure-tight` no estaba muerta sino sin adoptar: su comentario dice que estas clases existen para sustituir `max-width:XXch` inline, y había exactamente tres. Las otras tres se quedan por motivos distintos: `measure-lead` y `measure-hero` completan una escala de cinco medidas de lectura, y quitar peldaños de una escala la vuelve incoherente; `stat-editorial--dark` es la forma oscura de un rol vivo en 5 páginas, y sin ella el primer stat sobre fondo oscuro se escribiría en blanco a mano. CSS de `dist/`: 68.193 → 67.738 bytes. |
| DEC-D41 | Contenido | **El nivel S1 de `levels.ts` pasa a tener efecto: la plantilla combinada renderiza la `intersectionThesis` de esas intersecciones en un `Callout`.** Se borra `src/components/ui/SectionHeader.astro`, que no usaba ninguna página. | **APPROVED** | Instrucción del responsable 2026-09-11 | `SERVICE_ZONE_LEVEL` se importaba y no se consultaba: el sistema clasificaba las 48 combinadas en 4 S1, 43 S2 y 1 S3, y todas se renderizaban igual. Las cuatro tesis escritas **coinciden una a una con las cuatro S1**, y las cuatro son Alcalá de Henares, así que conectarlo no exige escribir nada: es el cuarto campo escrito y sin renderizar de esta tanda de trabajo, tras `localAngle`, `topWorks` y `metaDesc`. Para S2 y S3 no cambia nada, porque `levels.ts` prohíbe inventar tesis donde no hay dato. **Sólo tres de las cuatro reciben el bloque**: la cuarta, `reforma-cocinas × alcala-de-henares`, es una página escrita a mano que ya expone esa misma tesis y con más desarrollo —su primer condicionante es «Extracción: si la cocina no tiene salida directa al exterior»—, así que añadirlo duplicaría contenido contra §3. Copy nuevo: sólo el título del `Callout`, «Condicionante propio de {zona}», que es una etiqueta estructural y no una afirmación de negocio. `SectionHeader` se borra en vez de adoptarse: hay 56 bloques con su forma, pero 14 usan encabezados que su prop `size` no cubre y su subtítulo lleva estilos inline que el proyecto ya no admite. Conservarlo sugería que el patrón estaba resuelto cuando no lo está. |
| DEC-A15 | SEO | **Las 23 imágenes del sitio reciben `width` y `height` explícitos**, como exige CLAUDE.md §4. Las 10 de proyectos llevan medidas propias porque no comparten proporción. | **APPROVED** | Auditoría 2026-09-10 | Su ausencia provoca salto de maquetación al cargar (CLS), que es factor de Core Web Vitals. Detectado en la auditoría de paso pedida por el responsable. |

---

## E. Técnica y Código

| ID | Fase | Decisión | Estado | Fuente | Notas |
|---|---|---|---|---|---|
| DEC-E01 | Lote F | `SITE.whatsapp !== 'TU_TELEFONO'` condicional en Header.astro desktop, Layout.astro (flotante), contacto.astro. | **APPROVED** | Header.astro (commit b87ad27) | Sin condicional: se renderizaba `wa.me/TU_TELEFONO` roto. Lote F lo detectó y corrigió. |
| DEC-E02 | Lote F | `SITE.phone !== 'TU_TELEFONO'` condicional en Header.astro móvil, Footer, secciones de contacto. | **APPROVED** | Lote F audit | openingHoursSpecification en JSON-LD también condicionado. |
| DEC-E03 | Fundacional · corregida 2026-09-07 | **Sin frameworks de cliente ni hidratación de componentes.** El sitio es estático (Astro static output) y no sirve ningún bundle JS. Se permite JS inline mínimo y progresivo: el sitio debe funcionar completo con JavaScript desactivado. Excepciones vigentes: los tres `is:inline` de `Layout.astro` (clase `.js` y observer de reveals) y la lógica de cálculo de `calculadora-precio-reforma.astro`. | **APPROVED** | Architecture decision | La redacción anterior decía «0 KB JavaScript cliente», que era falsa: la home sirve ~5 KB de JS inline. Lo que sí es cierto y se conserva como regla es que no hay bundle — comprobado el 2026-09-07: **0 ficheros `.js` en `dist/`** — ni framework ni hidratación. Se reescribe para que la regla real sea auditable y una sesión no borre los reveals creyendo que cumple el canon. |
| DEC-E04 | Lote F | Todas las tablas de precios tienen wrapper `<div class="overflow-x-auto">`. | **APPROVED** | [slug].astro, [barrio].astro (commit b87ad27) | Sin wrapper, las tablas rompían el layout en mobile (375px). |
| DEC-E05 | Fundacional | `npm run build` + verificación obligatoria antes de cerrar cualquier tarea. El build debe completarse sin errores y sin placeholders expuestos en `dist/`. | **APPROVED** | CLAUDE.md §9 | Detecta errores de TypeScript, imports rotos, y placeholders que llegaron a la build. |
| DEC-E06 | Lote F | `aviso-legal.astro:33` y `privacidad.astro:32` usan literal `[Completar con domicilio social real]` en lugar de `{SITE.address}`. | **APPROVED** | Lote F (NOT YET COMMITTED) | SITE.address no es domicilio social legal. Los literales son placeholders visibles que recuerdan al titular que debe completar ese dato. **PENDIENTE COMMIT.** |
| DEC-E08 | Saneamiento | **Contrato de artefactos externos.** Todo fichero producido por una skill, un agente o una herramienta externa es NO NORMATIVO por defecto; su sitio es `docs/research/` y debe abrir con la cabecera de estado (ESTADO · Origen · Fecha). Una recomendación solo es regla cuando se registra como DEC con su verificación contra el código. Ficheros protegidos que ninguna skill edita: `CLAUDE.md`, `docs/DECISION-REGISTER.md`, `DESIGN-BLUEPRINT.md`, `src/data/calculator.ts`, `scripts/*`, `astro.config.mjs`. Ningún documento salvo `CLAUDE.md` declara precedencia o autoridad. | **APPROVED** | CLAUDE.md §11 + `scripts/audit-docs.mjs` | Es la causa raíz de las contradicciones desmontadas en este ciclo: `hypotheses.md` lo escribió una skill de SERP y contradecía §4; `DESIGN.md` lo generaba `impeccable` y acabó siendo autoridad tipográfica contra el código; `blueprint/trazzo360/*.yaml` describía un enlazado contrario a R6/R7. Ninguno mentía sobre su origen: no lo declaraba. Ahora declararlo es obligatorio y lo verifica `npm run audit:docs`, incorporado a `npm run audit`. Al implementarlo se detectaron y corrigieron cuatro autodeclaraciones de autoridad en las cabeceras de DECISION-REGISTER, TRAZZO360-SYSTEM, VISUAL-ROLLOUT-MAP y DESIGN-BLUEPRINT. La reaparición de `DESIGN.md` sale como advertencia, no como error, porque `impeccable` puede regenerarlo. |
| DEC-E09 | Saneamiento | **Protocolo de laboratorio.** El lab vuelve como método, con procedimiento: se crea en `src/pages/lab/`, nunca con sufijo `-lab` fuera de ahí; el lab **no es la fuente del canon** — los valores aprobados se extraen a `global.css` como tokens o clases de rol, nunca copiando estilos inline; la lista de divergencias de `audit:design` es el backlog de migración. Aislamiento en `scripts/lib/scope.mjs`, definición única que consumen los cinco auditores y `astro.config.mjs`. Enlace de producción hacia `/lab/` es error que rompe el build (R12). | **APPROVED** | `docs/PROTOCOLO-LAB.md` + `scripts/lib/scope.mjs` | Lo que falló antes no fue el lab sino la ausencia de procedimiento: acabó siendo fuente del ADN visual, se publicó y nadie sabía cuándo dejaba de ser experimento. Cada auditor tenía su propio criterio de exclusión o ninguno, y esa divergencia hizo que 24 errores críticos fueran en realidad páginas de laboratorio. Los cuatro aislamientos se verificaron con una página de prueba con violaciones deliberadas antes de darlos por buenos: no entra en sitemap (71 URLs), sale `noindex` incluso con indexación activa mientras la página de control es indexable, no aparece en ninguna de las cinco auditorías ni en `check`, y desaparece de `dist/` en build de producción. |
| DEC-E10 | Saneamiento | **`PRODUCT.md` vigilado por hash.** `audit:docs` compara el SHA-256 (16 primeros caracteres) del fichero contra `PRODUCT_APPROVED_SHA256`. Hash aprobado a 2026-09-08: **`383f6de9e2f22359`**. Si difiere, emite **advertencia, no error**: «PRODUCT.md regenerado: verifica que no ha reintroducido reglas. Ver §11.» Al actualizar el fichero legítimamente hay que actualizar la constante en el script y esta nota. | **APPROVED** | `scripts/audit-docs.mjs` | `PRODUCT.md` lo genera `impeccable` (lleva `<!-- impeccable:product-schema 1 -->`) y está en la lista blanca de la raíz, así que una regeneración con reglas dentro pasaría sin avisar. Se descartaron dos alternativas: detectar lenguaje normativo daría falsos positivos permanentes, porque el fichero ya contiene reglas legítimas —«Ningún precio en el sitio puede diferir de los rangos definidos»—, y un auditor que avisa siempre se ignora siempre; y hashear sólo el bloque marcado dependería de que `impeccable` mantenga su marcador, la misma fragilidad que se rechazó al elegir identificar los botones de WhatsApp por fichero y no por markup. Probado: modificar el fichero dispara la advertencia con el hash nuevo y mantiene exit 0. |

---

## Historial de Commits por Fase

| Commit | Hash | Contenido |
|---|---|---|
| Lote A | `46b795b` | ADN visual global + saneamiento de datos |
| Lote B | `5fa0474` | ADN visual hubs + datos de contenido |
| Lote C | `b70ecba` | Zonas 7 páginas + template condicional |
| Lote D | `af3f432` | S-level content + priceFrom + claims + schema |
| Lote E | `2b0e942` | Páginas restantes + claims sweep |
| Lote F | `b87ad27` | Auditoría global preproducción (HEAD actual) |

---

---

## F. Deuda Técnica Registrada (no blocker)

| ID | Deuda | Archivos afectados | Cuándo abordar |
|---|---|---|---|
| DT-F01 | ~~Em-dash en prosa editorial~~ | — | **CERRADA 2026-09-07.** Dejó de ser deuda al resolverse DEC-B02 vs DEC-B03: el `—` en prosa corrida es correcto y no hay nada que limpiar. Lo que sí se vigila —marcador de lista e interfaz— lo comprueba `npm run check`. |
| DT-F02 | Evolución V2 → V1 en hubs: Integrales, Pisos, Locales, Comunidades | 4 hubs de servicio | Post-lanzamiento, según priorización editorial |
| DT-F03 | Página 404 en V3 (visual legacy) | `src/pages/404.astro` | Post-lanzamiento |
| DT-F05 | `audit-content.mjs` no compara el recuento de palabras con los rangos de §3 y no mide boilerplate; DEC-C07 afirmaba lo contrario | `scripts/audit-content.mjs` | Cuando se decida si la medición merece implementarse. Enmendado en documentación mientras tanto |
| DT-F06 | La regla de radios y sombras tiene tres redacciones incompatibles: `DESIGN-BLUEPRINT.md` (cero absoluto), `docs/VISUAL-ROLLOUT-MAP.md` (excepciones en WhatsApp y header) y `docs/ANATOMIAS-CANONICAS.md` (cero). La implementación no coincide con ninguna. `--radius: 0px` está declarado y no lo consume nadie. Materia sin autoridad: se conserva el código actual y ninguna de las tres se aplica automáticamente | `DESIGN.md`, `DESIGN-BLUEPRINT.md`, `docs/VISUAL-ROLLOUT-MAP.md`, `docs/ANATOMIAS-CANONICAS.md`, `src/components/WhatsAppBtn.astro`, `src/components/Header.astro` | Cuando se decida la redacción única. Conservar el código actual no equivale a aprobar las excepciones que contiene |
| DT-F07 | `audit:design` valida tres propiedades tipográficas y su mensaje de éxito se lee como conformidad visual global | `scripts/audit-design.mjs` | Junto a DT-F08 |
| DT-F08 | `audit:schema` no valida la entidad única `#business` que exige CLAUDE.md §4 | `scripts/audit-schema.mjs` | Junto a DT-F07 |
| DT-F09 | **62 páginas repiten el anchor «Solicitar visita técnica gratuita» hacia `/contacto/`**, contra CLAUDE.md §4 («nunca el mismo anchor dos veces hacia la misma URL desde la misma página»). Es anterior a los CTA de DEC-D28, que llevan anchors propios y no contribuyen | hubs de servicio, combinadas, plantilla `[servicio]/[barrio].astro` | Cuando se decida el criterio de variación de anchors. Requiere reescribir copy de CTA, no es mecánico |

---

*Última actualización: 2026-09-10. Añadir nuevas decisiones en la sección correspondiente con ID secuencial por área.*
